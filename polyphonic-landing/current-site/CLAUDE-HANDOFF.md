# Current Polyphonic landing page — start here

Snapshot: September 9, 2026, after the rounded Luca logo update. This folder is the current landing page for transfer into the Polyphonic website. Older sibling landing directories are not the source of truth for this work.

## Editable source

`index.html` plus `assets/` are the complete page. The app simulation lives in `assets/demo.js` and `assets/demo.css`. It is static HTML/CSS/JS, not the desktop client and not React. Port from this code rather than regenerating from screenshots.

Run from this folder:

```sh
npm run build
npm start
```

Node 20+; these commands require no dependency install. The server serves `dist/`, so rebuild after edits. Default port is 8744; if the previous design preview is still using it, run `PORT=8745 npm start`. `npm ci` installs pinned verification tools, then `npm run verify` exercises the preview on port 8744. Do not accidentally test an older server. `dist/` and `node_modules/` are ignored.

## Latest decisions to preserve

- Keep the accepted page composition and the dot-matrix display language. Surfaces and type were retuned on 9 September 2026 (see below): warm, flat, light — Inter and JetBrains Mono, not Instrument Sans.
- Polyphonic is one home for agents with continuity, memory, history, and persistent Mnemos identity. Frame agents as collaborators, not task-named tools.
- Rounded Luca glyph is the Polyphonic mark: `assets/brand/polyphonic-solid.svg`, used in header/footer and favicon. Preserve the round stroke caps and joins.
- Existing agent glyph system stays. `assets/brand/agents.html` is an exploratory study Riley chose not to pursue; do not integrate its candidates into the product.
- The simulated rail has Projects and Agents, plus all six runtime marks. Northstar opens a second room-navigation column; selecting a room enters its conversation.
- Mira is the resident formerly called Research. Do not rename her back to a task.
- Conversation and right drawer are separate full-height cards; the conversation toolbar stays within its card. Split, popout, and rail collapse work in the demo.
- Do not reintroduce prototype character avatars or generated background artwork.
- Important illustrative interactions matter; full feature parity with the desktop app is not the goal.

**WP-02 · warm, flat, light (9 September 2026).** Typography, palette and flatness now follow the old Polyphonic prototype at `GLOBAL-DESIGN-DOCS/polyphonic-particle-field-landing/`: Inter at 200/300/400 (no weight above 500, the hero set entirely in Inter 200 on two lines, no Doto), JetBrains Mono in spaced caps for meta, one warm ink `rgb(236,232,224)` at .92/.72/.56/.40/.22 over `#0a0a0c` with surfaces `#0d0c0e` / `#121114` / `#17161a`, no borders on cards, panels, the app frame, the email input or the footer, every control the same outlined pill, and the hero field rendered in that warm ink with a `#c9a23a` glow at the peaks. Layout, copy, structure and JavaScript behaviour are unchanged.

**WP-02b · quiet band, true app mono (9 September 2026).** The marquee band is quiet, not gone: floor background, no hairlines, letters at .55 ink (`level` on the `marquee-crisp` preset in `assets/dot-display.js`), unlit lattice unchanged, no gold in the band. The demo inside the app frame follows the desktop app, which sets its UI in Inter Variable and its mono in Fragment Mono — so `--app-mono` (base.css) puts Fragment Mono back on every mono element in the demo, at the demo's own tracking and case, while the page's own meta stays JetBrains Mono in `.24em` caps. `scripts/email-assets.js` now names Inter 300 for the display line; the shipped email PNGs were not re-rendered.

**WP-02c · the frame is the product (9 September 2026).** Never restyle the app demo. `assets/demo.css` is the product's interior and is kept at its accepted content (plus the `--app-mono` swaps and the interior `font-weight: 400` baseline); `.app-frame` and `.d-popout` pin every token the interior consumes to the app's own values, so a page retune cannot leak in. Only the frame's outer edge — its border and shadow — belongs to the page. Measured against a build of the accepted commit: every interior colour identical (92/92 probes), frame-interior mean pixel difference 2.27 on 0-255 (glyph noise from Instrument Sans to Inter), and 0 style or geometry changes across the 345 elements outside the frame.

**WP-03 · the demo is the app (9 September 2026).** The demo's screens are the app's screens. Rail sections are `PROJECTS` / `MESSAGES` / `RUNTIMES`; Agents is the three-column personal-agent screen (seven fictional residents, each with its runtime's mark, plus a "6 runtimes connected" strip and the Documents / Notebook / Settings detail); Brain is the Connections screen with Northstar counts; Activity is the live-then-`RECENT` screen; project rooms and the conversation drawer take the app's dividers, system lines and section stack. Ziggy is now a direct message alongside Luca, Mira and Luca & Mira. Fixtures stay fictional and in the Northstar world — no resident names from the real app. `assets/demo.css` keeps WP-02c's interior untouched above a single appended WP-03 block that uses the pinned interior tokens; the page outside the frame is pixel-identical to the WP-02c build. Three fixture assertions in `scripts/verify-demo.mjs` were updated (each commented `WP-03`) because the brief deliberately replaced what they asserted.

**WP-04 · the words have a voice (10 September 2026).** The page stopped listing features and started telling one story, in order: the agents move in, Luca remembers, Luca asks Mira in front of you, Codex asks you before it touches a file. Copy is verbatim from `polyphonic-landing/current-site/COPY.md` — that deck, not this file, is the source of truth for every line; do not paraphrase it back. Two cards were re-cut. Agents now lists the five named residents (Luca, Mira, Ziggy, Iris, Wren), each under the mark of the runtime it runs on, with a live dot only on the two that are working. Brain is one agent, not a grid: your question, Luca’s reply, then four `role="switch"` source chips under **Luca can see** — the page’s own outlined pill, filled with `--s3` at ink .92 when on, outlined at ink .40 when off, focus brightening its own border. The Mira column and the `#research-status` branch are gone; the `grants`/`knowledge()` mechanism stayed and only its clauses changed. Each chip still carries `class="grant-toggle" data-agent="luca"` — not a style hook but the selector `assets/demo.js` reads to mirror the grants into the app frame; renaming it silently breaks the demo and `scripts/verify-demo.mjs`. `.brain-response` reserves the tallest reply (146px / 158px under 760) so toggling a chip never moves the chips beneath it. Hero, band caption, product caption, cards 3 and 4 and the beta block are text-only swaps; the meta/OG/Twitter descriptions carry the new hero sub. The app demo frame is untouched. One assertion block in `scripts/verify.mjs` was rewritten (commented `WP-04`) because it named the checkbox grid and the old empty-state line.

**WP-06 · seven windows (10 September 2026).** Card 1's list of agents is gone. In its place the beat-1 headline and body sit left-aligned in the container (body capped at 560px), and under them a full-bleed band drifts seven Polyphonic conversation windows to the left — one agent per runtime, each showing one finished exchange, every line verbatim from `COPY.md` → *Band · seven windows*. The band is a plain block in `<main>`, not inside `.container`, so it is viewport-wide without any `100vw` arithmetic; its own `overflow` keeps the document from scrolling sideways. The track is the seven windows plus an `aria-hidden` duplicate; the keyframe runs `translateX(0 → -50%)`, linear and infinite, at 24 px/s — the 24px gap is each window's own `margin-right`, so half the track is exactly 7 × 424 px = 2968 px and the loop never jumps. It pauses on hover, on `:focus-within` (the band itself is `tabindex="0"`), and under `html[data-motion="paused"]` from the page's motion toggle. Under `prefers-reduced-motion` and below 761px there is no animation at all: the band becomes a native horizontal scroller with snap, no mask, the duplicate hidden, and a visible end. Every interior value in a window was measured off the demo's real pop-out (`.d-popout`) in the running page — surfaces, radii, paddings, header height, name and body sizes, the composer bar — and `.pw-window` originally joined `.app-frame, .d-popout` in the interior-token pin — **WP-10 removed it from that pin**; the windows now take the page's own tokens and only their form is the product's. Luca, Mira and Ziggy carry the sigil marks cards 2–4 use; Iris, Wren, Otto and Nia take the demo's initial-in-a-square. `assets/demo.js` and `assets/demo.css` were not touched. One assertion block was added to `scripts/verify.mjs` (commented `WP-06`).

**WP-07 · the sign (10 September 2026).** The dot-matrix band no longer scrolls. The seven-window row owns horizontal motion on this page, so the band became a sign: a fixed display that changes what it says in place, and nothing in it ever moves sideways. Its canvas is `data-scene="sign"` with `data-phrase="ONE HOME FOR"` and a `|`-separated `data-names`; the scene, its preset and its frame pump all live in `assets/dot-display.js` (the old `marquee-crisp` scene in `assets/effects.js` is left intact and unused). Two slots sit either side of a gutter the width of the marquee's old ` · ` separator — three cells — and both are placed from the WIDEST name, so the phrase never moves when the name changes. Where that pair will not fit with three cells of air at each end — measured, below 972px — the phrase is dropped and the name stands alone, centred; below 432px the sign also steps its own pitch down to 5, and below 360px to 4, so the longest name still fits, because a static `CLAUDE CODE` at cell 6 is 396px wide and a phone is not. Timings: hold 3200ms, then a 700ms change — the outgoing name dims down the ink floor into the unlit lattice (never to black), the incoming starts 200ms in with each dot up to 120ms late, rises over 380ms to a `.68` bloom and settles back to the band's `.55` in 300ms; six names, 3900ms each, 23.4s a cycle, measured. The `.55` rest is exact, not approximate: the preset carries `level .68` for the bloom's headroom and `inkLit .26 × .95 / SIGN.REST` so a resting letter renders at the WP-02b band's own `rgb(129,127,123)` and 2.502px dot — a settled hold is pixel-identical to the old glass across the whole 1440×64 canvas. The sign drives its own rAF (`signPump`) rather than the page loop, because a change already in flight must be able to finish after the toggle stops everything else; it holds still when paused, when the tab is hidden and when scrolled out of view (0 frames), it never starts under reduced motion (the settle frame is the whole of it), and it skips repainting a hold it has already drawn. The canvas is `aria-hidden`; one static `.sr-only` sentence next to it carries the full line and never changes as the sign cycles. `assets/site.css` lost the band's two 9% edge fades — they hid a marquee entering and leaving, and with nothing entering they only ate the ends of the phrase at container widths; the band's box, height and background are unchanged. One assertion block was added to `scripts/verify.mjs` (commented `WP-07`); `scripts/verify-demo.mjs` had no marquee assertions to update.


**WP-08 · the loop has depth (10 September 2026).** The seven-window row reads as a loop rolling past the viewer instead of a flat belt: whatever crosses the centre is closest — flat, full size, lit — and toward either edge a window turns away, drops back and descends into shadow. Depth is a function of screen position, never of window index, so the picture is continuous while the loop drifts and exact the instant it stops. `assets/site.js` writes one custom property per window, `--d` = (window centre − viewport centre) ÷ half the viewport, clamped to [−1,1]; `assets/site.css` derives `--a: d²`, `--left` and `--right` from it and nothing else. `d` comes from the drift animation's own `currentTime` (via `track.getAnimations()`) and the track's fixed 424px pitch, so no frame reads layout — measured: 0 `getBoundingClientRect` calls on the band, track or windows across 312 frames; the pitch, window width, the track's 2968px span and the band's resting left edge are read once and again on resize (a rect fallback exists only if the animation is unavailable). The band carries `perspective:1400px` with its origin at the viewport centre and the track `transform-style:preserve-3d` so the windows — its flex items — actually enter that perspective; each window is `translateZ(-160px × a) rotateY(9deg × d)`, no `scale()`, which at the viewport edge is 9° turned and 324px of a 400px window. Light comes from the centre: a `::after` shade darkens the *far* edge first (`.42 × left²` / `.42 × right²`) over a uniform `.22 × a`, and the window's inset top-edge highlight scales with nearness (`.10 × (1 − a)` — 0.1 at the centre, 0 at the edge). The band's edge mask is eased in four stops so a window descends into the dark rather than being cut off it, and a radial whisper at 2.5% white sits on the floor behind the centre. The loop runs only while it can change anything: it stops when the band leaves the screen (IntersectionObserver, 0 frames measured over 2.5s) and when the drift is paused by hover, `:focus-within` or the motion toggle — writing once more so the frozen picture is exact (`--d` unchanged over 10s in all three cases). Cost: 0.37ms per frame averaged over 5s at 1440, 0 long tasks. Desktop drift only: below 761px and under `prefers-reduced-motion` there is no perspective, no transform and no shade — those scrollers are byte-for-byte the flat row WP-06 built (band MAD 0.06 and 0.03 against the WP-07/09 build). The band's markup, copy, drift speed, pause hooks and window interiors are untouched, and so is `assets/demo.*`.

**WP-10 · the windows join the page (10 September 2026).** The seven band windows keep their form and take the page's material. `.pw-window` is out of the interior-token pin — that pin is right for `.app-frame` and `.d-popout`, which *are* the product, and wrong for an illustration on a marketing page, where it put a second colour system (warm-grey surfaces, flat neutral `#c7c6bc` ink, lighter than anything the page owns) in the middle of the first. Nothing about `.app-frame` / `.d-popout` moved: 12 elements probed in each against a build of `57529e431` are identical on all 20 pinned tokens and 10 computed properties, and the hero frame screenshot is byte-identical. The 6px bezel, the 1px outline and the black ring are gone; the visible object is head + body at radius 14, lifted by exactly two shadow layers (`0 2px 6px rgba(0,0,0,.45)`, `0 26px 60px rgba(0,0,0,.5)`) and nothing else. Surfaces are the page's: head `--s2`, body and compose `--s1`, composer field `--s2`, your-line bubble `--s3`, and the runtime pill and send chip `--s3`. Ink is the page's ramp — base `.72`, name `.92` at weight 400, bubble `.92`, placeholder 13px at `.22`, the three window dots `rgba(var(--ink),.14)` — and the runtime pill takes the page's own meta style, JetBrains Mono caps at 9px / `.24em` / ink `.40`, not the app's Fragment Mono (the initial-in-a-square avatar moved with it). The head keeps a 1px bottom rule at `var(--line2)`: with only five levels between `--s2` and `--s1` the tone step alone stops reading as a title bar at 400px, and the rule is the page's own hairline value, not the WP-06 outline. WP-08's depth is untouched — same `--d`, same `rotateY`/`translateZ` at an identical frozen instant — except that its two `::after` shade constants were scaled by 0.7 (`.42 → .29` directional, `.22 → .154` uniform) because they were tuned against surfaces four times lighter and were crushing the turned windows: at the viewport edge the body line goes from 3.67:1 back to 4.75:1, and the centre is unaffected (the shade is 0 there). The nearness light survives as a designed depth cue but moved onto `.pw-head` — with the bezel gone the head's own background is the window's top edge, so an inset shadow on `.pw-window` would be painted over — and is warm now: `inset 0 1px 0 rgba(var(--ink), calc(.08 * (1 - var(--a))))`, measured .08 / .05 / 0 at d = 0 / ±0.589 / ±1. The band's centre glow is warm too. Structurally, `.pw-transcript` is a flex column justified to `flex-end` so the exchange settles on the composer with the empty space above it, and the repeated agent name inside the transcript is gone — the title bar already names the speaker. Copy, drift, pitch, pause hooks, the mask, the reduced-motion and ≤760px scrollers, `assets/demo.*` and `assets/site.js` were not touched, and no verify assertion named anything that changed.

## Website integration and signup (wired 2026-09-09)

The page ships as static files at **polyphonic.chat/beta** inside the Polyphonic web app repo (`Riley-Coyote/polyphonic-v2`, `public/beta/`, deployed by Lovable). Rebuild and re-copy after any edit here:

```sh
BASE_PATH=/beta/ PUBLISH=1 \
SIGNUP_ENDPOINT=https://kknchdnrujzheulqzowv.supabase.co/functions/v1/beta-signup \
PRIVACY_URL=https://polyphonic.chat/privacy npm run build
# then copy dist/ over polyphonic-v2/public/beta/ and push main
```

`PUBLISH=1` drops the preview `noindex` tag; `BASE_PATH` rewrites asset URLs; the signup endpoint and privacy URL are written into `dist/assets/config.js` (source `assets/config.js` stays empty). A plain `npm run build` is still the local preview.

Signup backend lives in polyphonic-v2 Supabase: `beta_signups` table, public `beta-signup` edge function (validation, honeypot field `website`, per-IP rate limit, duplicate → `already_subscribed`), and service-role-only `beta-invite`, which sends the download email (`_shared/email-templates/beta-invite.tsx`) through the existing transactional email queue. Share card: `assets/share/polyphonic-beta.png` (source `scripts/share-card.html`); email images: `assets/email/` (source `scripts/email-assets.html`). Both render from the preview server with Playwright.

Copy decisions: no support matrix. The page says "Start with Claude Code or Codex for the best experience" and the marquee reads "One home for …" rather than "Works with …".

## Provenance and verification

Copied from the active `polyphonic-landing-production` design folder, not an older prototype. The original preview remains available separately. Source assets were compared by hash during transfer. Audit documents describe prior passes; they are historical evidence, not a fresh certification of every interaction. Selected screenshots are included for layout reference. Exploratory logo studies are not production requirements.
