# C32 task receipt

- Status: `PASS`
- Dependencies: C29, C30, and C31 passed on
  `codex/brain-connections-v1-2-1`.
- Exact installed product checkpoint:
  `ec5ef6fbe6256dd1651280d818976b346045389d`
- Evidence closure: this `Finalize V1.2.1 release evidence` commit; its exact
  SHA is the accepted local `luca/v1.1` branch head.
- Bundle: `com.luca.agent-network.dev`, Developer ID Application
  `Riley Ralmuto (WQUY4M5HYR)`; strict deep verification passed.
- Installed executable SHA-256:
  `601c182ba2678f671a1356256041801f692792c62b9e80a3c81a9720e0596254`
- A223: PASS — bounded metadata-only discovery and explicit connection found
  one isolated repository, one Codex history, and one Claude Code history.
- A224: PASS — repository filtering, session redaction, original reread/hash
  verification, incremental watcher refresh, and launch reconciliation passed.
- A225: PASS — encrypted bindings/indexes persisted with no plaintext absolute
  path, repository/session body, or fixture canary in the store files.
- A226: PASS — default explicit grants covered all current residents and the
  future-resident path; revoke, stale, reconfirm, unavailable, disconnect, and
  reconnect cases passed.
- A227: PASS — authorization preceded index decryption and source reads;
  imported and connected candidates shared ranking, deduplication, and the
  existing eight-section/24 KiB turn bounds.
- A228: PASS — repository tools used desktop-owned session capabilities and
  relative paths; traversal, symlink escape, `.git` mutation, stale sessions,
  disconnected sources, and descendant capability inheritance failed closed.
- A229: PASS — read operations were automatic; one patch and command were
  approved, one patch was rejected, and permission decisions expired at their
  exact scoped operation.
- A230: PASS — one separately approved local commit respected hooks; no push,
  remote mutation, pull-request, or credentialed Git tool exists.
- A231: PASS — Brain presents quiet Repositories, Codex, Claude Code, and Files
  connection cards with consent, details/activity, loading, empty, failure,
  focus, keyboard, mobile, and screen-reader states in the Void/Luca system.
- A232: PASS — exact-build native acceptance with real Hermes and OpenClaw
  proved recall, repository search/read, watcher refresh, approval/denial,
  disconnect, relaunch persistence, normal DMs, and mixed-room health.
- A401: PASS — focused formatting, strict Clippy, protocol, Tauri, desktop,
  Playwright, workspace, web, and mobile preflights passed.
- A402: PASS — the exact product checkpoint was rebuilt, Developer-ID signed,
  atomically installed, relaunched, and exercised through the installed app.
- A403: PASS — connections, grants, indexes, and cursors persisted; receipts
  remained process-memory-only; messaging remained healthy.
- A404: PASS — protected Hermes/OpenClaw configuration, credential, model,
  memory, workspace, and schedule hashes were byte-identical; original fixture
  hashes were unchanged and the encrypted-store plaintext scan was clean.
- A405: PASS — the one formal `just ci` run completed on unchanged product
  checkpoint `ec5ef6fbe6256dd1651280d818976b346045389d` after native acceptance.
- Regression repair: refresh, disconnect, and unchanged reconnect now creates
  fresh domain-separated page lineages and cannot revive tombstoned pages or
  replay prior source/binding idempotency records.
- Final local state: acceptance override cleared, additive synthetic histories
  removed, exact installed app relaunched against the normal profile, relay
  running, product tree unchanged after the formal gate.
- Remote state: not pushed; no pull request opened.
