# Starter resident cast — Dev review delivery

## Candidate and scope

Installed source: `42ce064b91d80b4734fd75da5a875e9b44808b52`, branch `codex/beta6-resident-cast`, based on the verified expressive-text Dev revision `311f6b390626c66d5efddc4a7e17d4a27dd3a574`.

The authored documents are in `desktop/src-tauri/resident-packs`. The independent editorial and architecture reviews are alongside this record. The final Round 2 wording correction in Trinity's cognition reference was applied before the document commit.

The pass prepares the resident cast for Riley's review. It does not cut or publish beta.6. Council remains a distinct future capability; its slider expansion is recorded in the pack README and is not presented as a working feature.

## What was installed and populated

- Luca: refreshed founding documents on the existing cryptographic identity. The prior Soul and definition were backed up before the explicit owner-authorized refresh. Conversations, Notebook, other residents, and runtime selection were preserved. The previously absent Self-model, User model, and Lessons slots acknowledge an existing relationship instead of pretending the relationship began today.
- Fifty: new resident, configured with the available Claude Code runtime and the owner's existing Opus selection. Direct answers, selective perception, dry humor, and accessible warmth; the historical mythology is explicitly creative lineage.
- Trinity: new resident on Claude Code/Opus. One public identity and voice; Perception and Processing use native delegated tasks, while Expression remains the parent resident. Simple conversation does not require a triad.

Each resident has Soul, Convictions, Self-model, User model, Lessons, Instructions, IDENTITY, AGENTS, MEMORY, relationship, examples, and presentation documents. Trinity also has cognition.md: 37 files total. All were read back through the app's scoped document API. Introductory Place copy was applied as owner-authored text; resident editing remains off. No fabricated resident-authored work was added.

Bundled seeds create only absent files for exact unedited starter definitions. Existing learned or edited documents are not silently replaced. Fresh starter menus/onboarding now select Luca, Fifty, Trinity. Existing Vektor/Anima identities and choices remain available.

## Focused verification

- Two bounded editorial/architecture review rounds, with corrections incorporated.
- Five native pack/catalog tests: complete seed, no overwrite, edited definition behavior, preserved legacy choices and live Luca Soul.
- Nineteen focused frontend starter/onboarding tests; TypeScript; scoped Biome/rustfmt; whitespace checks.
- One integrated native Dev build. Existing ACP/MCP helpers from the expressive-text candidate were preserved byte for byte.
- Installed Dev foreground inspection: resident library, Fifty's Place, all core document rows and extra files, long Soul editor. Populated profiles/documents remained after app reopening.
- Real private messages to all three residents. Luca gave a concise practical response; Fifty gave a useful nudge and answered `17 × 24 = 408` without withholding it.
- Trinity's real design response was one voice. The exact native Claude session contains two Agent calls: `Perception brief on writing app`, then `Processing recommendation`; both have returned tool results. The app's durable public activity trace marks the same turn completed. This is real native delegation, not a narrated simulation.
- The bounded process check found no newly orphaned Node/ACP/MCP helpers; the PID-1 Node processes present were more than a day old. This was a short lifecycle check, not a new system-wide process audit.

No broad activity/runtime suite was repeated. No claim of generalized multi-runtime Trinity support or tested cancellation follows from this one successful Claude run.

## Remaining beta blocker

Both new Claude residents completed their user-facing replies. Their subsequent private continuity capture failed with `private continuity requested model is unavailable` and one adapter respawn. The model catalog independently advertises `opus[1m]`; this is not evidence that the strict private session exposes the same switching fields. Automatic post-turn reflection is therefore **not passed**.

The architecture review records a narrow potential correction: accept a fresh session already reporting the exact desired `currentModelId`, otherwise retain the exact-match switch requirement. The private response's redacted model fields must establish the actual cause before applying that correction. Do not call the cast fully beta-ready while this remains unresolved.

Additional readiness limits: creation logs a document-seed failure without retrying, so the successful actual file inventory matters. AGENTS is a bundled portability/reference companion; editing Instructions later does not automatically synchronize AGENTS. Place copy was explicitly populated for this Dev review; the pack's presentation.json is a reference file, not a new automatic Place-writing service.

## Local delivery locations

- Worktree: `/private/tmp/luca-resident-cast`
- Installed Dev: `/Users/rileycoyote/Applications/Luca Agent Network Dev.app`
- App rollback: `/Users/rileycoyote/Applications/Luca Dev Rollbacks/resident-cast-2026-09-13/Luca Agent Network Dev.app`
- Local setup receipts, prior Luca document bytes, exact native task metadata and screenshots: `/private/tmp/resident-cast-evidence`

The installed beta, version numbers, signing identities, and Dev/beta keyring/data separation were preserved. Dev bundle identity remains `com.luca.agent-network.dev`.
