# K05D — Persist authoritative revision heads and lifecycle state

Status: complete — authoritative restart state and immutable desktop lease passed
Gate: G2.2
Lane: runtime_cognition
Owner: g2_k05_impl
Reviewer: g2_k05_review
Depends on: K03D, K05
Owned files: trusted-desktop revision authority schema/store seams and narrowly required pure-ledger snapshot APIs
Forbidden files/decisions: plaintext bodies, revision-order inference, messaging dependency, UI, shared protocol changes
Commit: `00ffef50` (desktop persistence slice A), `226a91c5` (immutable lease)
Repair count: 2 (explicitly authorized 2026-08-05)

## Acceptance

- [x] A209

## Checks

- Restart head/lifecycle reconstruction.
- Archive and forget non-resurrection.
- Atomic encrypted-record plus revision-authority transition CAS.
- Zero-write immutable active-generation snapshot.

## Evidence

- Read-only seam audit:
  `evidence/G2/G2.2/K05D/reconnaissance.md`.
- Initial pure projection review:
  `evidence/G2/G2.2/K05D/review-block.md`.
- Pure projection re-review:
  `evidence/G2/G2.2/K05D/projection-review-pass.md`.
- Complete restart/CAS contract:
  `evidence/G2/G2.2/K05D/restart-contract.md`.
- Restart snapshot review block:
  `evidence/G2/G2.2/K05D/restart-snapshot-review-block.md`.
- Authorized restart snapshot re-review:
  `evidence/G2/G2.2/K05D/restart-snapshot-review-pass.md`.
- Desktop slice-A review block:
  `evidence/G2/G2.2/K05D/desktop-slice-a-review-block.md`.
- Authorized desktop slice-A repair pass:
  `evidence/G2/G2.2/K05D/desktop-slice-a-review-pass.md`.
- Final immutable-lease security review:
  `evidence/G2/G2.2/K05D/immutable-lease-security-review-pass.md`.

## Result

- The pure restart snapshot and hydration interface has passed independent
  review and is frozen for desktop implementation.
- Desktop persistence, generation CAS, strict restart hydration, and the
  complete-owner atomic replacement seam passed independent review in
  `00ffef50`.
- The AppState-owned immutable read lease captures one exact scoped authority
  generation, releases the lifecycle lock only for bounded decrypt/retrieval,
  and revalidates epoch, generation, key version, root fingerprint, and
  restore state before any borrowed plaintext consumer runs.
- Existing encrypted database/WAL/SHM state requires existing custody and can
  never mint a replacement key during boot or read recovery.
