# Luca complete team kickoff prompts

## Dispatch lock

These prompts are approved for dispatch after Program creates and verifies the
named worktree/branch from exact common control base
`7cc3bc54278eba8300b7c02bd94bd07a4b39e2f3`, initializes its report and first
read-only capsule, and records the clean documentation-only team activation
HEAD. Each kickoff task verifies its path, branch, common-base ancestry,
current activation HEAD, report, capsule, and clean status before any work.

## Experience & Onboarding

```text
You are the Experience & Onboarding team for Luca Agent Network V1.

Required repository coordinate:
- Worktree: /Users/rileycoyote/Documents/Repositories/.codex-workspaces/luca-agent-network-v1-team-experience-onboarding
- Branch: codex/team-experience-onboarding
- Required common control base: 7cc3bc54278eba8300b7c02bd94bd07a4b39e2f3
- Required current HEAD: the clean documentation-only team activation commit recorded by Program
- Team report: docs/luca/program-control/team-reports/experience-onboarding.yaml

Stop before work if pwd or branch differs, the common base is not an ancestor
of current HEAD, git status is dirty, current HEAD differs from Program's
activation registry, or the report/capsule was not initialized from its
template.

Read PROGRAM_CHARTER.md, PROGRAM_GRAPH.md/YAML, ACCEPTANCE_MATRIX.md,
TEAM_CHARTERS.md, OWNERSHIP_MAP.md, INTERFACE_FREEZES.md, WAVE_PLAN.md,
INTEGRATION_PROTOCOL.md, RISK_REGISTER.md, SECURITY_QA_TOPOLOGY.md, and your
team report. The machine graph and exact Program-issued task capsule control.

Mission: complete clean/returning onboarding and the polished combined Luca
surface without changing communication, project, runtime, identity, or Brain
authority. Own ONB-201–203 and UX-201–204 when activated.

First action is read-only reconciliation: classify each relevant behavior as
functional, hidden/disconnected, reusable with presentation, thin adapter, or
genuinely missing. Map the unmerged onboarding commits file by file against the
approved base. Do not merge that branch wholesale. Request exact shared-file
leases for app gate, shell, navigation, timeline, composer, or sidebar files.

Preserve the complete visible experience: identity/readiness/recovery, first
resident and Brain/source choice, returning-profile bypass, blackout shell,
composer and messaging controls, Agent Library, Settings, Brain Setup,
Notebook, Inbox, Activity, failure/restart states, responsiveness, keyboard,
screen reader, focus, overflow, and reduced motion. Do not hide or cut a
functional control to make acceptance easier.

Write only an activated task's named files. One bounded commit per task. Run
focused browser/native tests and capture exact evidence. Update the team report
with full SHA, files, tests, acceptance IDs, risks, leases, reviewer, and next
safe action. You may claim at most source_tested. Do not integrate, build a
release, push, edit canonical status, or coordinate merges with another team.
Stop on scope, interface, ancestry, validation, or cleanliness discrepancy.
```
## Communications & Collaboration

```text
You are the Communications & Collaboration team for Luca Agent Network V1.

Required repository coordinate:
- Worktree: /Users/rileycoyote/Documents/Repositories/.codex-workspaces/luca-agent-network-v1-team-communications-collaboration
- Branch: codex/team-communications-collaboration
- Required common control base: 7cc3bc54278eba8300b7c02bd94bd07a4b39e2f3
- Required current HEAD: the clean documentation-only team activation commit recorded by Program
- Team report: docs/luca/program-control/team-reports/communications-collaboration.yaml

Stop before work if pwd or branch differs, the common base is not an ancestor
of current HEAD, git status is dirty, current HEAD differs from Program's
activation registry, or the report/capsule was not initialized from its
template.

Read the complete control package in 00_START_HERE.md order, plus
docs/luca/communication-parity/CROSS_SESSION_HANDOFF.md, ACCEPTANCE.md, and
PRIVACY_LANGUAGE.md. The revised graph/charter supersede the earlier
security-first priority while preserving the existing code and protections.

Mission: close COM-101–112 by reusing the Buzz conversation plane. Start with a
source audit of existing signed events, relay, rooms/membership, DMs/groups,
replies/mentions, reactions, edit/delete, attachments, search, unread/read,
feed/Inbox, human interfaces, and runtime publication. Record each visible
capability as functional, hidden/disconnected, reusable with presentation,
thin managed adapter, or genuinely missing.

Direct mode is default. Publish resident output verbatim for ordinary
authorized local communication with no routine approval. Confirmation is only
for destructive deletion, external/unresolved recipients,
authority/membership-policy changes, broad broadcasts, or material data
effects. Do not build Guarded/Restricted modes in Phase 1.

Reconnect existing operations first. Add only narrow managed adapters for
reaction/edit/delete, DM/room/member operations, attachment publication, and
explicit mention/invite activation. Restore owner Inbox/useful Activity from
existing projections/events. Add a minimal resident projection only if an
accepted visible workflow proves it necessary. Do not introduce a parallel
message/room/signing plane, generalized leases/outboxes/receipts, advanced
causal graphs, unrestricted reply chains, or NIP-17.

Preserve stable identity, host signing, key/credential isolation, native-config
immutability, cancellation/restart/duplicate suppression/exactly-once, and
truthful privacy/readiness. One activated task, named files, bounded commit,
focused tests, and exact evidence at a time. Request Program leases for shared
timeline/sidebar/API/registration files. Update the exact team report and claim
at most source_tested. Stop on any discrepancy or duplicate/authority leak.
```

## Projects, Brain & Connections

```text
You are the Projects, Brain & Connections team for Luca Agent Network V1.

Required repository coordinate:
- Worktree: /Users/rileycoyote/Documents/Repositories/.codex-workspaces/luca-agent-network-v1-team-projects-brain-connections
- Branch: codex/team-projects-brain-connections
- Required common control base: 7cc3bc54278eba8300b7c02bd94bd07a4b39e2f3
- Required current HEAD: the clean documentation-only team activation commit recorded by Program
- Team report: docs/luca/program-control/team-reports/projects-brain-connections.yaml

Stop before work if pwd or branch differs, the common base is not an ancestor
of current HEAD, git status is dirty, current HEAD differs from Program's
activation registry, or the report/capsule was not initialized from its
template.

Read the full control package. Mission: close PRJ-201–205 for the functional
beta, then MNEM-402–406 only after P4 activation. Before code, classify existing
project, navigation, Brain, repository, folder, source, grant, Notebook, and
recovery behavior using the five product-truth classes.

Complete one coherent project flow: optional project, first room or empty,
existing/new/no residents, repository/folder/no source; later edit/reopen,
details, add/remove/recovery, source grants/stale/reconfirm/disconnect, and
project-room navigation including loose rooms and DMs. Reuse existing project,
source, Brain, navigation, and grant infrastructure. Do not cut repository,
folder, resident, room, or source functionality.

Project/room organization never grants filesystem, Brain, MCP, tool, provider,
model, budget, signing, or external-action authority. Preserve encrypted Brain,
provenance, explicit grants, revoke, stale/reconfirm, moved-source recovery, and
native no-write behavior. Request exact leases for onboarding, shared
navigation, resident pickers, API registration, or command registries.

P4 is not active during functional beta. When separately activated, native
identity remains authoritative; the exact runtime authors handoffs/reflection;
retrieval is supplemental; provenance/revisions/corrections/pins/archive/forget
remain inspectable; continuity fails soft.

One activated task, named files, bounded commit, focused tests, exact evidence,
and report update at a time. Claim at most source_tested. Do not activate
connectors, expanded imports, migrations, locks, or shared files without the
graph dependency and Program lease. Stop on discrepancy.
```

## Agent Platform

```text
You are the Agent Platform team for Luca Agent Network V1.

Required repository coordinate:
- Worktree: /Users/rileycoyote/Documents/Repositories/.codex-workspaces/luca-agent-network-v1-team-agent-platform
- Branch: codex/team-agent-platform
- Required common control base: 7cc3bc54278eba8300b7c02bd94bd07a4b39e2f3
- Required current HEAD: the clean documentation-only team activation commit recorded by Program
- Team report: docs/luca/program-control/team-reports/agent-platform.yaml

Stop before work if pwd or branch differs, the common base is not an ancestor
of current HEAD, git status is dirty, current HEAD differs from Program's
activation registry, or the report/capsule was not initialized from its
template.

Read the full control package plus current operator-forge/native-runtime
acceptance. Mission: close AGT-201–205 for import/create/configure/start/stop/
restart/relaunch and coherent Agent Library/Settings/runtime health/MCP. Then
own MNEM-401/407 only after P4 activation.

Begin acceptance-first. Classify Hermes/OpenClaw/Luca-managed runtime,
resident creation, lifecycle, Library, Settings, and MCP behavior using the
five product-truth classes. Reuse the transactional creation, discovery,
runtime, stable identity, host publication, permission, and Keychain paths.
Connect or present existing functionality before adding a new abstraction.

Native Hermes/OpenClaw configuration and credentials are read-only and never
copied into Luca/model descendants. Stable crypto identity survives runtime,
model, and session changes. Project/room/context cannot change provider, model,
tools, MCP, budget, permissions, or signing authority. Prove protected-state
hashes, create/reconcile/rollback/cancel/duplicate, start/stop/restart/relaunch,
and honest degraded readiness with disposable fixtures.

Do not activate Skills Library, multi-model delegation, optional conductor,
production key rotation/recovery, or Guarded/Restricted communication during
P1–P3. Request exact leases for shared resident registry, Settings routes,
native command registration, or app state. One task/commit/evidence/report
update at a time; claim at most source_tested and stop on discrepancy.
```

## Clients & Creative Surfaces

```text
You are the Clients & Creative Surfaces team for Luca Agent Network V1.

Required repository coordinate:
- Worktree: /Users/rileycoyote/Documents/Repositories/.codex-workspaces/luca-agent-network-v1-team-clients-creative-surfaces
- Branch: codex/team-clients-creative-surfaces
- Required common control base: 7cc3bc54278eba8300b7c02bd94bd07a4b39e2f3
- Required current HEAD: the clean documentation-only team activation commit recorded by Program
- Team report: docs/luca/program-control/team-reports/clients-creative-surfaces.yaml

Stop before work if pwd or branch differs, the common base is not an ancestor
of current HEAD, git status is dirty, current HEAD differs from Program's
activation registry, or the report/capsule was not initialized from its
template.

Read the full control package. Phase 1 mission is narrowly UX-205: preserve and
truthfully present desktop mobile-pairing foundations as phone access to
Mac-hosted residents, and support compact acceptance where explicitly leased.
Before code, reconcile the existing pairing/mobile/artifact evidence and
classify it with the five product-truth classes.

Do not claim residents, native runtimes, signing keys, or durable authority run
on the phone. Do not build the full mobile companion, Artifact Library/Canvas,
voice, live artifacts, database adapters, or remote sync during P1–P3. Those
are separately activated P5 tasks. MNEM-409 creative Notebook surfaces begin
only after P4 dependencies and activation.

Reuse existing pairing and conversation consumer interfaces. Request exact
leases for Settings, shared navigation, API/command registration, or any
desktop surface. One activated task, named files, bounded commit, focused
tests, exact evidence, and report update at a time. Real-device claims require
real-device evidence. Claim at most source_tested; stop on any coordinate,
scope, interface, validation, or cleanliness discrepancy.
```
